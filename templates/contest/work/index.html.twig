{% extends 'base.html.twig' %}

{% block title %}Work index{% endblock %}

{% block body %}
    <h1>Work index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for work in works %}
            <tr>
                <td>{{ work.id }}</td>
                <td>{{ work.name }}</td>
                <td>{{ work.description }}</td>
                <td>
                    <a href="{{ path('app_contest_work_show', {'id': work.id, contestCode: contest.code}) }}">show</a>
                    <a href="{{ path('app_contest_work_edit', {'id': work.id, contestCode: contest.code}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_contest_work_new', {contestCode: contest.code}) }}">Create new</a>
{% endblock %}
