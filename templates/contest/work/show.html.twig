{% extends 'base.html.twig' %}

{% block title %}Work{% endblock %}

{% block body %}
    <h1>Work</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ work.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ work.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contest_work_index', {'contestSlug': contest.slug}) }}">back to list</a>

    <a href="{{ path('app_contest_work_edit', {'id': work.id, 'contestSlug': contest.slug}) }}">edit</a>

    {{ include('contest/work/_delete_form.html.twig') }}
{% endblock %}
