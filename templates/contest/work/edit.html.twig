{% extends 'base.html.twig' %}

{% block title %}Edit Work{% endblock %}

{% block body %}
    {{ include('contest/_contest_header.html.twig', {'contest': contest}) }}

    <h1>Edit Work</h1>

    {{ include('contest/work/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_contest_work_index', {contestSlug: contest.slug}) }}">back to list</a>

    {{ include('contest/work/_delete_form.html.twig') }}
{% endblock %}
