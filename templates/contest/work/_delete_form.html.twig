<form method="post" action="{{ path('app_contest_work_delete', {'id': work.id, 'contestSlug': contest.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ work.id) }}">
    <button class="btn">Delete</button>
</form>
